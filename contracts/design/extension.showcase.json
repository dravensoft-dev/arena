{
  "$extensions": {
    "com.dravensoft.arena": {
      "grouping": "figure-ground",
      "job": "invite and convert: marketing, commerce, onboarding, pricing, a landing page somebody arrives on"
    }
  },
  "r-surface": {
    "$type": "dimension",
    "$value": "{r.xl}",
    "$description": "A product card is a thing you want to touch, so its corner is softer than a panel\nyou want to read. One step up the scale rather than an invented number, which is the rule an\nextension holds to everywhere: it re-values a role, and the value it moves to is still Arena's."
  },
  "r-surface-floating": {
    "$type": "dimension",
    "$value": "{r.lg}",
    "$description": "The floating tier moves with the surface tier and stays one step below it, so a menu\nstill reads as lighter than the card it opens over. Moving one without the other is the\nhalf-applied look the two roles exist to prevent."
  },
  "r-control": {
    "$type": "dimension",
    "$value": "{r.md}",
    "$description": "A softer button. Not a pill: a pill reads as a tag or a filter chip, and a call to\naction that looks like a filter is a call to action nobody presses."
  },
  "r-control-sm": {
    "$type": "dimension",
    "$value": "{r.sm}",
    "$description": "A checkbox box and a segment move with the buttons, one step below them, which is\nthe relationship roles.json authored and not a second decision taken here. Left behind, a 4px\ncheckbox beside a 10px button reads as a control the extension forgot rather than as a small\none, and it is the same half-applied look r-surface-floating exists to prevent one tier up."
  },
  "r-field": {
    "$type": "dimension",
    "$value": "{r.md}",
    "$description": "Fields follow controls, because a form reads as one object when its inputs and its\nsubmit button share a corner."
  },
  "r-marker": {
    "$type": "dimension",
    "$value": "{r.sm}",
    "$description": "A price badge sits on a rounder card, so a square-cornered marker would read as\nstuck on rather than part of it."
  },
  "bw-surface": {
    "$type": "dimension",
    "$value": {
      "value": 0,
      "unit": "px"
    },
    "$description": "The change this extension is actually about. Arena groups by drawing the region,\nwhich is cheap in pixels and right for dense data; here the grouping is carried by elevation\nand air instead, which is the Gestalt trade between common region and figure/ground.\n\nSafe to remove because a surface is not a user interface component: WCAG 1.4.11 asks 3:1 of a\nCONTROL's boundary and of a meaningful graphic, and a card is neither. bw-control and bw-field\nare therefore untouched below, and bw-separator with them, because a table that lost the rules\nbetween its rows when its frame went would stop being readable."
  },
  "shadow-surface-rest": {
    "$type": "shadow",
    "$value": {
      "offsetX": {
        "value": 0,
        "unit": "px"
      },
      "offsetY": {
        "value": 1,
        "unit": "px"
      },
      "blur": {
        "value": 0,
        "unit": "px"
      },
      "spread": {
        "value": 0,
        "unit": "px"
      },
      "color": {
        "colorSpace": "srgb",
        "components": [
          1,
          1,
          1
        ],
        "alpha": 0.12
      },
      "inset": true
    },
    "$description": "What replaces the border in DARK, and the reason this extension varies with the\ntheme at all. A drop shadow is a darkening, so it separates a light card from a light page and\ndoes almost nothing between a dark card and a dark page.\n\nThe obvious alternative was measured and refused: moving the card onto a different step of the\nsurface scale. The widest step Dravensoft's base ramp offers in dark is base-300 on base-100 at\n1.13:1, and neutral on base-100 at 1.22:1. Neither is an edge anybody sees, and the ramp belongs\nto the consumer, so an extension cannot widen it.\n\nA rim light inverts the direction instead. White at 12% over base-200 reads about 1.5:1 against\nthe page, because it is light on dark rather than dark on dark. It is inset along the top edge\nand not a ring, which matters: a ring would be a hairline drawn around a region, which is the\nmechanism this extension traded away. An edge lit from above is a statement about an object\nstanding in light, so it is still figure and ground."
  },
  "light": {
    "shadow-surface-rest": {
      "$type": "shadow",
      "$value": "{shadow.1}",
      "$description": "In light a darkening is a boundary again, so the ordinary drop shadow is the\nanswer and it REPLACES the rim rather than joining it: two depth cues on one edge read as a\nseam. The lightest step of the elevation scale, because a page of product cards each carrying a\ndialog's depth reads as a pile rather than a grid."
    }
  },
  "shadow-control-raised": {
    "$type": "shadow",
    "$value": "{shadow.3}",
    "$description": "A call to action that lifts further than anything else on the page. This is where\na voice whose job is to invite and convert spends most of its emphasis, and it is spent on one\nelement per view, which is the same rule as one primary accent per view."
  },
  "lift-control": {
    "$type": "dimension",
    "$value": {
      "value": 2,
      "unit": "px"
    },
    "$description": "Two pixels, and no more: the travel has to read as an answer to the pointer rather\nthan as the layout moving. It is a length rather than an alias because the base value is 0 and\nthere is no scale step for a distance a control travels."
  },
  "pad-surface": {
    "$type": "dimension",
    "$value": "{sp.8}",
    "$description": "20px to 32px, which is the other half of the air this voice spends. Ground around\na figure and room inside it are one decision made twice: a card given more space on the page and\nnone inside it reads as a crowded object standing alone, which is worse than either.\n\nThe floating tier is untouched, and that is the point of the split: a dialog roomed up by the\nsame ratio would start pushing its own buttons past the fold."
  },
  "rhythm-component": {
    "$type": "dimension",
    "$value": "{sp.6}",
    "$description": "The ground a figure needs around it. This voice took the line off a surface and\nasked elevation and air to carry the grouping, and air is half of that sentence: a card with no\nborder and no room around it reads as a stripe of a longer surface rather than as an object.\n\nrhythm-group is deliberately left alone. The air INSIDE a group is what makes a label and its\nfield read as one thing, and widening it would loosen the figure while the ground grew, which is\nthe opposite of what figure and ground means."
  },
  "rhythm-section": {
    "$type": "dimension",
    "$value": "{sp.10}",
    "$description": "Section air grows further than component air, so the ladder stays a ladder: group\nis 12, component becomes 24 and section becomes 40, which reads 1 to 2 to 3.3 where the default\nreads 1 to 1.33 to 2. A voice that widened one step without the other would flatten the very\nhierarchy the steps exist to state."
  },
  "dur-hover": {
    "$type": "duration",
    "$value": "{dur.mid}",
    "$description": "Slower than Arena's default, which is the whole difference between an interface\nthat acknowledges a pointer and one that responds to it. Still inside the transition range, so\nnothing here becomes motion a person waits on."
  },
  "ease-hover": {
    "$type": "cubicBezier",
    "$value": "{ease.in-out}",
    "$description": "The other half of the same decision, and the half a duration cannot make. Arena's\ndefault curve leaves at full speed and settles, which is an interface answering the instant the\npointer lands. This one starts from rest, which is what a thing with mass does: 220ms on that\ncurve reads as an object being moved, where 220ms on the default reads as the same instant answer\ntaking longer than it should.\n\nDerived from the principle rather than chosen beside it. A figure standing off a ground is an\nOBJECT, and an object has inertia; a voice that grouped by drawing a region has nothing to give\nmass to, which is why the default curve is right there and wrong here. Still one of Arena's three\nsteps, because the value a role moves to is the language's and not the voice's."
  },
  "press-scale": {
    "$type": "number",
    "$value": 0.96,
    "$description": "0.98 to 0.96, which is the return trip of the two pixels lift-control spends. A\ncontrol that rises under the pointer and then sinks under the press has completed a gesture; one\nthat rises and then barely moves has started one.\n\nTwice the default and no further. Past about 0.95 a button reads as a soft thing being squashed\nrather than a solid one being pressed, and this voice is about objects standing on a floor rather\nthan about toys."
  },
  "ff-eyebrow": {
    "$type": "fontFamily",
    "$value": "{font.display}",
    "$description": "The single most visible thing this voice does, and the one that was unreachable\nbefore the role tier grew. On a shop's card the eyebrow reads \"Single origin, Huehuetenango\": it\nis marketing copy, the first thing said about the product, and it belongs in the face the brand\nspeaks in.\n\nSet in mono it reads as telemetry. That is what this voice looked like everywhere outside its\ngeometry: the same eyebrow, the same capitals, the same ink as a console, with rounder corners.\nA voice that invites and one that reports cannot share a register and still be told apart."
  },
  "tt-eyebrow": {
    "$type": "keyword",
    "$value": "none",
    "$description": "Capitals travel with the mono face and leave with it. A product name's antetitle\nset in the display face and still shouting would read as a label somebody forgot to restyle,\nwhich is the half-applied look the role tier exists to prevent."
  },
  "track-eyebrow": {
    "$type": "number",
    "$value": "{ls.normal}",
    "$description": "The third of the same decision. Open tracking pays for capitals and costs\nlegibility without them.",
    "$extensions": {
      "com.dravensoft.arena": {
        "cssUnit": "em"
      }
    }
  },
  "ink-eyebrow": {
    "$type": "color",
    "$value": "{color.secondary}",
    "$description": "The primary accent is what a page spends on its one primary action, and the\nSKILL.md rule that a view carries a single primary accent is what makes that action findable. An\neyebrow on every card in a grid spends it twelve times before the reader reaches the button.\n\nSecondary is the assignment, not a colour: which of the consumer's two brand hues an antetitle\ntakes. A shop that swapped its palette gets its own secondary here and the rule still holds."
  },
  "step-title-surface": {
    "$type": "dimension",
    "$value": "{fs.h3}",
    "$description": "A product name leads its card. At h4 it sits at the same weight as the note under\nit and the card reads as a paragraph with a bold first line; at h3 it is the thing the eye lands\non and everything else is support.\n\nOne step, and only on a SURFACE title. step-title-section is untouched, so raising the card does\nnot raise the section head above it and flatten the page into one size."
  },
  "lh-heading": {
    "$type": "number",
    "$value": "{lh.snug}",
    "$description": "A title raised a step needs its lines pulled in, or a two-line product name reads\nas two products. 1.15 rather than the 1.5 every heading inherits, which is the leading of prose\nand was never chosen for a title.\n\nNot lh.tight: 0.98 puts a display face's own lines through each other, and check:extensions holds\nthe floor at 1."
  },
  "track-heading": {
    "$type": "number",
    "$value": -0.03,
    "$description": "Large type needs negative tracking or it falls apart: the space designed for a\n19px title is a gap at 52px, and this voice raises fs-h1 to 52 and fs-display to 80. Tighter than\nls.tight, which is the last step the scale has, so it is authored as a length rather than aliased.\n\nA rare case of a role authored outright, and the reason is that the scale stops where the type\nsizes this voice reaches begin.",
    "$extensions": {
      "com.dravensoft.arena": {
        "cssUnit": "em"
      }
    }
  },
  "gap-items": {
    "$type": "dimension",
    "$value": "{sp.4}",
    "$description": "The members of a group given a step more air, which is the inside half of the\nsame decision rhythm-section makes on the page. A voice that separates its sections generously\nand then packs the cards inside them has spent the air where nobody reads it."
  },
  "fill-page": {
    "$type": "color",
    "$value": "{color.base-100}",
    "$description": "Declared for completeness rather than for effect, and the distinction is worth\nkeeping honest. This voice claims figure and ground, and before this role it could name the figure\nand not the ground. It names both now.\n\nWhat it does NOT do is fix the separation. The fill step available above the page measures 1.13:1\nin Dravensoft's dark palette, which is invisible, and the ramp belongs to the consumer rather than\nto this voice. The rim light is still the signal that works. Declaring the floor lets a consumer\nwith a wider ramp get a figure that stands on a fill, and leaves this palette exactly where it was."
  },
  "fs-h1": {
    "$type": "dimension",
    "$value": {
      "value": 52,
      "unit": "px"
    },
    "$description": "A page heading that carries a page rather than titling a panel. The base ladder\naccelerates gently because a console has many peers on screen at once; a landing has few, so the\njump from the body it sits over is wider here.\n\nThe reading sizes below h2 do not move, in this extension or in any other. Body and chrome text\nis the one thing every voice shares, because how large a paragraph is set is a legibility\ndecision and not an expressive one. An extension widens the hierarchy by raising what shouts,\nnever by shrinking what is read."
  },
  "fs-display": {
    "$type": "dimension",
    "$value": {
      "value": 80,
      "unit": "px"
    },
    "$description": "The step a hero headline is set at. 80 over 64 is a fifth again, which reads as a\ndifferent kind of page rather than as the same page with bigger type."
  },
  "fs-hero": {
    "$type": "dimension",
    "$value": {
      "value": 120,
      "unit": "px"
    },
    "$description": "The largest step anything is actually set at, and the reason fs-hero was kept when\nit had no consumer."
  }
}
