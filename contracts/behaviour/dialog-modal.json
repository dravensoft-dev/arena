{
  "name": "dialog-modal",
  "source": "https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/",
  "description": "A window that covers the app and takes the whole interaction until it is dismissed. More than one component binds it, which is the point: written once here it is one contract each of them answers, rather than the same rules implied separately at each. Which components those are is each one's own <Name>.behaviour.json, because a binding travels with the component and a list here is stale the day one of them moves to a pattern of its own.",
  "requires": {
    "roles.element": "dialog",
    "roles.aria-modal": "true",
    "roles.label": "aria-labelledby or aria-label",
    "focus.onOpen": "first-focusable",
    "focus.onClose": "restore-invoker",
    "focus.trap": true,
    "keyboard.Escape": "close"
  }
}
