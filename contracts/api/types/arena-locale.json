{
  "name": "ArenaLocale",
  "kind": "object",
  "description": "The words Arena draws itself and the locale it formats dates in, provided once for a tree of components. Each word resolves to the component's own member when one answers it, then to the provided locale, then to the default here. A field whose default carries a {name} is a sentence with a value in it: Arena replaces each {name} it knows, so a language may put them in any order.",
  "fields": {
    "locale": { "form": "primitive", "type": "string", "required": true, "default": "en-GB", "description": "A BCP 47 tag. Every date Arena formats uses it; hour labels keep a 24-hour clock whatever it says." },
    "calendarToday": { "form": "primitive", "type": "string", "required": true, "default": "Today", "description": "The calendar button that returns to the current period." },
    "calendarPrevious": { "form": "primitive", "type": "string", "required": true, "default": "Previous", "description": "Names the calendar button that moves back one period." },
    "calendarNext": { "form": "primitive", "type": "string", "required": true, "default": "Next", "description": "Names the calendar button that moves forward one period." },
    "calendarRegion": { "form": "primitive", "type": "string", "required": true, "default": "Schedule, {range}", "description": "Names the calendar's region. {range} is the range title." },
    "calendarGrid": { "form": "primitive", "type": "string", "required": true, "default": "Schedule grid, {range}", "description": "Names the calendar's grid. {range} is the range title." },
    "calendarEventActions": { "form": "primitive", "type": "string", "required": true, "default": "Actions", "description": "Names the button that opens a calendar event's action panel." },
    "paginationPrevious": { "form": "primitive", "type": "string", "required": true, "default": "Previous", "description": "Names the pager's previous-page button." },
    "paginationNext": { "form": "primitive", "type": "string", "required": true, "default": "Next", "description": "Names the pager's next-page button." },
    "tableSortBy": { "form": "primitive", "type": "string", "required": true, "default": "Sort by", "description": "Labels the sort select a table draws in its card layout." },
    "tableEmpty": { "form": "primitive", "type": "string", "required": true, "default": "No data.", "description": "What a table shows when no row is written and its empty slot is not filled." },
    "toastClose": { "form": "primitive", "type": "string", "required": true, "default": "Close", "description": "Names a dismissible toast's close button." },
    "toastPinned": { "form": "primitive", "type": "string", "required": true, "default": "Pinned", "description": "The mark a toast that does not dismiss itself carries." },
    "toastPinnedHint": { "form": "primitive", "type": "string", "required": true, "default": "Does not auto-dismiss", "description": "The title on the pinned mark." },
    "sheetClose": { "form": "primitive", "type": "string", "required": true, "default": "Close", "description": "Names a dismissible sheet's close button." },
    "alertDismiss": { "form": "primitive", "type": "string", "required": true, "default": "Dismiss", "description": "Names a dismissible alert's close button." },
    "tagRemove": { "form": "primitive", "type": "string", "required": true, "default": "Remove", "description": "Names a removable tag's remove button." },
    "skeletonLabel": { "form": "primitive", "type": "string", "required": true, "default": "Loading", "description": "Names a skeleton's status region." },
    "spinnerLabel": { "form": "primitive", "type": "string", "required": true, "default": "Loading", "description": "Names a spinner when its own label is absent." },
    "switchConfirmHint": { "form": "primitive", "type": "string", "required": true, "default": "Requires confirmation", "description": "The title on the guard glyph of a switch that asks before it changes." },
    "bulkActionBarLabel": { "form": "primitive", "type": "string", "required": true, "default": "Actions on the selection", "description": "Names the bulk action toolbar." },
    "bulkActionBarClear": { "form": "primitive", "type": "string", "required": true, "default": "Clear", "description": "The text of the button that clears the selection." },
    "bulkActionBarClearLabel": { "form": "primitive", "type": "string", "required": true, "default": "Clear selection", "description": "Names the button that clears the selection." },
    "bulkActionBarCount": { "form": "primitive", "type": "string", "required": true, "default": "{count} {noun} selected", "description": "The selection count. {count} is drawn in bold; {noun} is the bar's noun." },
    "bulkActionBarNoun": { "form": "primitive", "type": "string", "required": true, "default": "items", "description": "What is counted, plural, when the bar's own noun is absent." },
    "commandPaletteDialog": { "form": "primitive", "type": "string", "required": true, "default": "Command palette", "description": "Names the command palette's dialog." },
    "commandPaletteSearch": { "form": "primitive", "type": "string", "required": true, "default": "Search commands", "description": "Names the search field when no placeholder names it." },
    "commandPalettePlaceholder": { "form": "primitive", "type": "string", "required": true, "default": "Search for an action or project…", "description": "The search field's placeholder when the palette's own is absent." },
    "commandPaletteList": { "form": "primitive", "type": "string", "required": true, "default": "Commands", "description": "Names the list of commands." },
    "commandPaletteEscape": { "form": "primitive", "type": "string", "required": true, "default": "ESC", "description": "The key hint beside the search field." },
    "commandPaletteEmpty": { "form": "primitive", "type": "string", "required": true, "default": "No results for \"{query}\".", "description": "What the list shows when nothing matches. {query} is what was typed." },
    "onboardingBack": { "form": "primitive", "type": "string", "required": true, "default": "Back", "description": "The button that returns to the previous step." },
    "onboardingSkip": { "form": "primitive", "type": "string", "required": true, "default": "Skip", "description": "The button that leaves the tour." },
    "onboardingNext": { "form": "primitive", "type": "string", "required": true, "default": "Next", "description": "The button that moves to the next step." },
    "onboardingDone": { "form": "primitive", "type": "string", "required": true, "default": "Got it", "description": "The button that closes the last step." },
    "onboardingStep": { "form": "primitive", "type": "string", "required": true, "default": "Step {current} of {total}", "description": "Names a step that has no title and no eyebrow. {current} counts from one; {total} is the number of steps." },
    "onboardingProgress": { "form": "primitive", "type": "string", "required": true, "default": "Progress: step {current} of {total}", "description": "Names the progress dots. {current} counts from one; {total} is the number of steps." },
    "confirmDialogEyebrow": { "form": "primitive", "type": "string", "required": true, "default": "Confirm", "description": "The confirmation's eyebrow when its own is absent." },
    "confirmDialogConfirm": { "form": "primitive", "type": "string", "required": true, "default": "Confirm", "description": "The confirming button when its own label is absent." },
    "confirmDialogCancel": { "form": "primitive", "type": "string", "required": true, "default": "Cancel", "description": "The cancelling button when its own label is absent." },
    "confirmDialogRequire": { "form": "primitive", "type": "string", "required": true, "default": "Type \"{text}\" to confirm", "description": "The prompt above the field a confirmation requires. {text} is what must be typed." },
    "errorStateTitle": { "form": "primitive", "type": "string", "required": true, "default": "Something went wrong", "description": "An error state's title when its own is absent." },
    "avatarOnline": { "form": "primitive", "type": "string", "required": true, "default": "online", "description": "Names the online presence dot." },
    "avatarBusy": { "form": "primitive", "type": "string", "required": true, "default": "busy", "description": "Names the busy presence dot." },
    "avatarAway": { "form": "primitive", "type": "string", "required": true, "default": "away", "description": "Names the away presence dot." },
    "avatarOffline": { "form": "primitive", "type": "string", "required": true, "default": "offline", "description": "Names the offline presence dot." },
    "lineChartName": { "form": "primitive", "type": "string", "required": true, "default": "{label} — line chart", "description": "A line chart's accessible name. {label} is the chart's label." },
    "barChartName": { "form": "primitive", "type": "string", "required": true, "default": "{label} — bar chart", "description": "A bar chart's accessible name. {label} is the chart's label." },
    "horizontalBarChartName": { "form": "primitive", "type": "string", "required": true, "default": "{label} — horizontal bar chart", "description": "A horizontal bar chart's accessible name. {label} is the chart's label." },
    "pyramidChartName": { "form": "primitive", "type": "string", "required": true, "default": "{label} — horizontal bar chart", "description": "A pyramid chart's accessible name, which reads as the horizontal bars it draws. {label} is the chart's label." },
    "doughnutChartName": { "form": "primitive", "type": "string", "required": true, "default": "{label} — doughnut chart", "description": "A doughnut chart's accessible name. {label} is the chart's label." },
    "doughnutChartPieName": { "form": "primitive", "type": "string", "required": true, "default": "{label} — pie chart", "description": "The same chart's accessible name in its pie shape. {label} is the chart's label." },
    "radarChartName": { "form": "primitive", "type": "string", "required": true, "default": "{label} — radar chart", "description": "A radar chart's accessible name. {label} is the chart's label." },
    "scatterChartName": { "form": "primitive", "type": "string", "required": true, "default": "{label} — scatter chart", "description": "A scatter chart's accessible name. {label} is the chart's label." },
    "chartTableCategory": { "form": "primitive", "type": "string", "required": true, "default": "Category", "description": "The first heading of the data table a categorical chart writes." },
    "chartTablePoint": { "form": "primitive", "type": "string", "required": true, "default": "Point", "description": "The first heading of the data table a line chart writes." },
    "chartTableAxis": { "form": "primitive", "type": "string", "required": true, "default": "Axis", "description": "The first heading of the data table a radar chart writes." },
    "chartTableSeries": { "form": "primitive", "type": "string", "required": true, "default": "Series", "description": "The first heading of the data table a scatter chart writes." },
    "doughnutChartLegend": { "form": "primitive", "type": "string", "required": true, "default": "Doughnut chart legend", "description": "Names a doughnut chart's legend." },
    "doughnutChartPieLegend": { "form": "primitive", "type": "string", "required": true, "default": "Pie chart legend", "description": "Names the legend in the pie shape." }
  }
}
