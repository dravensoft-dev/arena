{
  "component": "ArenaProgressBar",
  "description": "Determinate progress by default; indeterminate for a wait with no percentage.",
  "affordances": [],
  "api": {
    "progressPercentage": {
      "form": "primitive",
      "type": "number",
      "default": 0,
      "description": "How far along, 0-100. Clamped and rounded. Ignored when `indeterminate`."
    },
    "indeterminate": {
      "form": "primitive",
      "type": "boolean",
      "default": false,
      "description": "A wait with no percentage; the bar sweeps instead of filling."
    },
    "tone": {
      "form": "enum",
      "type": "ArenaProgressTone",
      "default": "accent",
      "description": "The bar's colour."
    },
    "label": {
      "form": "primitive",
      "type": "string",
      "required": true,
      "description": "Names what is progressing. Drawn above the bar, and it is the bar's accessible name. Required and guarded rather than defaulted: nothing can derive what is progressing, and a fallback of \"Progress\" satisfies roles.label mechanically while telling a screen-reader user only what the component is -- two of them on one page announce identically."
    },
    "showLabel": {
      "form": "primitive",
      "type": "boolean",
      "default": true,
      "description": "Draws the label above the bar. False leaves the bar alone and keeps the accessible name, which is carried by aria-label on the progressbar element rather than by the text. For a bar in a table cell or a card row, where the row already names what is progressing and repeating it is noise. `label` stays required either way, on the reading it already carries: what a screen reader announces is not a decision about what is drawn. It is the same escape ArenaIconButton.showLabel offers, and it is here because the two components pose one question."
    },
    "showPercentage": {
      "form": "primitive",
      "type": "boolean",
      "default": true,
      "description": "Shows the percentage beside the label. Determinate only."
    },
    "size": {
      "form": "enum",
      "type": "ArenaControlSize",
      "default": "md",
      "description": "The bar's thickness."
    }
  }
}
