{
  "component": "ArenaSpinner",
  "description": "Indeterminate wait indicator. For a measurable process use ArenaProgressBar instead.",
  "affordances": [],
  "api": {
    "size": { "form": "enum", "type": "ArenaControlSize", "default": "md",
              "description": "Diameter. 'sm' is --icon-sm exactly, so a spinner at that size sits inline with control text." },
    "tone": { "form": "enum", "type": "ArenaSpinnerTone", "default": "accent",
              "description": "Colour of the ring. 'on-accent' inside a filled button; 'accent' on a page surface." },
    "label": { "form": "primitive", "type": "string", "description": "Accessible name, announced by the status role. Say what is loading when you can. Absent, the provided locale's spinnerLabel answers it, which reads Loading by default." }
  }
}
