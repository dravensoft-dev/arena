{
  "component": "ArenaTooltip",
  "description": "A short label revealed on pointer intent. Bone over dark for contrast. The tooltip waits before appearing and before withdrawing, so a pointer crossing a toolbar reveals nothing.",
  "affordances": ["focus", "hover"],
  "api": {
    "label": { "form": "primitive", "type": "string", "required": true,
               "description": "The bubble's text. Arena draws the bubble; the consumer names it." },
    "content": { "form": "slot", "required": true,
                 "description": "The element the tooltip describes and attaches to." }
  }
}
