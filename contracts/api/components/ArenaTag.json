{
  "component": "ArenaTag",
  "description": "A pill for filters, technologies and statuses. A tone for what state a thing is in or a ramp slot for which thing it is, and an optional dismiss.",
  "affordances": [],
  "api": {
    "content": { "form": "slot",
                 "description": "The tag's label." },
    "tone": { "form": "enum", "type": "ArenaTagTone", "default": "neutral",
              "description": "The tag's emphasis colour. Ignored while `colorId` names a ramp slot, because a tag draws one colour and the two mean different things." },
    "colorId": { "form": "enum", "type": "ArenaCatSlot",
                 "description": "An identity colour from the categorical ramp, the ramp the charts and the calendar read, so one entity keeps its colour across a chart, a schedule and a label. Colour here means which thing and never what state, which is why it replaces `tone` rather than joining it: a label reading \"Backend\" is not a warning, and a tag that could say both at once would say neither. Optional, and its absence is the tone tag. The slot's colour also reaches the tag as a custom property, `--arena-tag-cat`, so an appearance that fills the marker rather than outlining it is a style plugin's to write and needs no member here." },
    "removable": { "form": "primitive", "type": "boolean", "default": false,
                   "description": "Whether the dismiss × is shown. Every layer gates the × on this member and never on whether anything listens for `remove`, because Arena never derives what it draws from what a consumer listens for. Removability is a declared input, not something inferred from the event." },
    "disabled": { "form": "primitive", "type": "boolean", "default": false,
                  "description": "Whether removal is unavailable while the tag stays visible: a filter a consumer's permissions lock, not a tag that is merely inert. It reflects through `aria-disabled` rather than the native `disabled` attribute, so the × keeps its place in the tab order and a screen-reader user is told the action is unavailable instead of never finding it. With `removable` false there is no × and nothing to disable." },
    "remove": { "form": "event",
                "description": "The dismiss × was activated. Never emitted while `disabled`." }
  }
}
