{
  "component": "ArenaEmptyState",
  "description": "A placeholder for an empty collection: an icon, a title, a message, and an optional action.",
  "affordances": [],
  "api": {
    "icon": { "form": "primitive", "type": "string",
              "description": "A Phosphor class name for the glyph Arena draws, muted." },
    "title": { "form": "primitive", "type": "string", "required": true,
               "description": "The headline: what is empty." },
    "headingLevel": { "form": "enum", "type": "ArenaHeadingLevel", "default": "h3",
                      "description": "Which rung of the document outline the title takes. Only the element changes: the title's class is the same at every value, so the render is identical and no appearance follows from it. It defaults to `h3`, the card rung of the title ladder, because an empty state fills the body of a region something above it already names, so its headline sits under that name rather than beside it. `none` is refused at runtime, the rule every component whose `title` is required follows: a title required because it names the thing it draws cannot also be told that the name is not one." },
    "message": { "form": "primitive", "type": "string",
                 "description": "A sentence of guidance under the title." },
    "action": { "form": "slot",
                "description": "A single call-to-action control, centred under the message." }
  }
}
